import TableLayout from './TableLayout.vue';
import { ComponentConfig } from '@/components/registry';

export default {
  id: 'table-layout',
  name: 'Table Layout',
  icon: 'list', // You might want to define a specific icon
  component: TableLayout,
  options: {}, // Define any layout-specific options here
} as ComponentConfig;
